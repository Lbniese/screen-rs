# Release

`screen-rs` will not install a binary named `screen` unless the user explicitly
chooses to replace GNU Screen.

Any release with a compatibility claim must name the reference GNU Screen
version and provide a passing compatibility matrix.
