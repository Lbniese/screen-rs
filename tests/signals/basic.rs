//! Basic signal handling integration tests for screen-rs.
//!
//! These tests verify that screen-rs correctly handles signals such as
//! SIGHUP (detach clients), SIGTERM/SIGINT (graceful shutdown), and
//! parent PID monitoring.

// No signal integration tests yet.
// Signal handling is exercised through the session lifecycle tests.
